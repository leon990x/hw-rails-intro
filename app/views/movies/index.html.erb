<!--  This file is app/views/movies/index.html.erb -->
<h2>All Movies</h2>

<%#  Part 2: Start here... %>

<table class="table table-striped col-md-12" id="movies">
  <thead>
    <tr>
      <th>Movie Title</th>
      <th>Rating</th>
      <th>Release Date</th>
      <th>More Info</th>
    </tr>
  </thead>
  <tbody>
    <% @movies.each do |movie| %>
      <tr>
        <td class="<%= ('hilite' if @sort == 'title') %>">
          <a id="title_header" onClick="<%= link_to 'Movie Title',movies_path(sort: 'title',ratings:@ratings),id:"title_header" %>">
          <%= movie.title %></a>
        </td>
        <td>
          <%= movie.rating %>
        </td>
        <td class="<%= ('hilite' if @sort == 'release_date') %>">
          <a id="release_date_header" onClick="<%= link_to 'Release Date',movies_path(sort: 'release_date',ratings:@ratings),id:"release_date_header" %>">
          <%= movie.release_date %></a>
        </td>
        <td>
          <%= link_to "More about #{movie.title}", movie_path(movie) %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
<%= link_to 'Add new movie', new_movie_path, :class => 'btn btn-primary' %>